# Local MIDI Library

Place personal `.mid` or `.midi` files in this directory. KermitPiano scans this folder at startup and when **Refresh Library** is selected.

Files imported from elsewhere can be copied here without overwriting an existing song; duplicate names receive a numeric suffix.
