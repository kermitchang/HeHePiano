# KermitPiano 開發日誌

最後更新：2026-08-04

## 專案狀態

- Git repository 已建立並推送至 `origin/main`。
- 最新 commit：`813fd5c feat: initialize KermitPiano practice engine`
- 最新完整驗證：`./gradlew build` 成功。
- Compose Desktop 已完成啟動 smoke test。

## 已完成階段

### Stage 5–12：遊戲基礎與 UI

- Timeline Engine：Game Clock、Song Time、Waterfall Position 分離；支援播放、暫停、重播、速度與 loop 的基礎架構。
- Judge：Perfect、Good、Miss、Combo。
- KeyboardInput：電腦鍵盤輸入與 Z／X 八度切換。
- Song Repository：內建 DemoSong。
- Canvas Waterfall：只繪製可見音符。
- 單元測試、README、架構整理。
- 88-key Piano：A0～C8（MIDI 21～108）、PianoModel、PianoViewport、PianoLayout 分離。
- Practice、Full 88、Follow Song Viewport。
- 深色節奏遊戲 UI、左右手視覺區分、Debug Overlay。

### Stage 13：Playable MIDI Practice Engine

- Standard MIDI File Parser：Type 0／1、VLQ、Running Status、Note On／Off、Velocity 0、Tempo、Time Signature、Key Signature、Track Name、Program Change、Control Change、End Of Track。
- Tempo Map 在 Import 時轉為 microseconds，播放時不重算 tick。
- MIDI Analysis：歌曲資訊、軌道、平均音高與 LEFT／RIGHT／IGNORE 建議。
- Open MIDI → Analyze → Import Song 流程。
- `PlayableSong` 介面與未來 `MidiInput` 邊界。
- MIDI parser 已以本機測試檔驗證；個人或第三方 MIDI 檔案不會納入版本控制。

### Stage 14：Responsive UI、Local MIDI Library、Audio Foundation

- Top Bar responsive policy：Wide、Compact、Narrow 三種寬度模式；窄視窗將次要控制項收進 More Menu。
- 遊戲區以同一內容寬度建立 Waterfall 與 PianoLayout；鍵盤高度約為內容高度 26%。
- `PianoDeviceProfile`：Computer Keyboard、AK490 49 Keys、Custom Range。
- Practice／AK490：預設 C3～C7，共 49 MIDI notes；八度移動會限制於 A0～C8。
- Local Song Library：`source/midi/` 掃描、Refresh、`.mid`／`.midi` 過濾，以及同名檔案數字 suffix 規則。
- SoundFont 文件結構：`source/soundfonts/`；SoundFont 與 sample 檔案已加入 `.gitignore`。
- `PianoAudioEngine`、NoAudio、FakeAudio、PlayerInputAudioRouter，以及 JVM FluidSynth process backend 基礎。
- Player Input Note On／Off 會直接轉送給 AudioEngine；Restart 與歌曲切換會送出 All Notes Off。
- SoundFont 尋找會依序檢查 JVM property、`KERMITPIANO_SOUNDFONT`、專案預設檔與目錄中的 fallback；Debug 面板可顯示診斷資訊並播放 Test C4。
- SoundFont 目錄預設忽略所有使用者提供的音源與 sample，只保留 README 與 `LICENSE-*.txt`，避免授權受限的資產進入 Git。

## 目前資料夾

```text
source/
├── midi/
│   └── README.md
└── soundfonts/
    ├── README.md
    ├── LICENSE.example.txt
    └── LICENSE-<name>.txt
```

## 目前限制與下一步

- 未安裝 `fluidsynth` 或未提供合法 `.sf2` 時，App 會安全降級為 NoAudio 模式。
- 尚未實作 USB MIDI、Audio Guide Sound、SoundFont chooser／設定持久化、錄音、混音器、AI Coach 或 Raspberry Pi 打包。
- FluidSynth backend 尚未完成實際音色與 Input-to-Audio latency 量測，不能宣稱低延遲已驗證。

## 之後若要測試鋼琴聲

1. 手動安裝 FluidSynth，例如 macOS：`brew install fluid-synth`。
2. 將授權清楚的 `.sf2` 放到 `source/soundfonts/piano.sf2`。
3. 在 `source/soundfonts/LICENSE.example.txt` 記錄來源與授權；有原始授權文字時，保留為 `LICENSE-<name>.txt`。
4. 執行 `./gradlew :composeApp:run`。

## 驗證指令

```shell
./gradlew build
./gradlew :composeApp:run
```
